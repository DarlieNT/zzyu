# NodeLoc OAuth2 配置
# 在 https://conn.nodeloc.cc/apps 注册应用后获取以下信息

# OAuth2 客户端配置
NEXT_PUBLIC_OAUTH_CLIENT_ID=your_client_id_here
OAUTH_CLIENT_SECRET=your_client_secret_here

# 回调地址 (根据部署环境修改)
# 本地开发环境
NEXT_PUBLIC_OAUTH_REDIRECT_URI=http://localhost:3000/api/auth/callback
# 生产环境 (替换为你的域名)
# NEXT_PUBLIC_OAUTH_REDIRECT_URI=https://your-domain.vercel.app/api/auth/callback

# NextAuth 配置 (可选，用于会话加密)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-here

# 环境标识
NODE_ENV=development